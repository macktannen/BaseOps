rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /flights/{flightId}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
    match /receipts/{flightId}/{expenseId}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
