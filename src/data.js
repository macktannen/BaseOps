// Legacy mock data — all arrays are empty.
// Kept as empty module to prevent Rolldown scope hoisting TDZ errors.
// All data now comes from Firestore via DataProvider.
export const mockPilots = [];
export const mockAircrafts = [];
export const mockAccounts = [];
export const mockCustomZones = [];
export const mockFlights = [];
export const mockVendors = [];
